# 静态资源

- 将 `echarts.min.js`（5.4.3）放到此目录后，K 线页将优先使用本地文件，减少对 CDN 的依赖。
- 下载方式：浏览器打开 https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js 另存为 `echarts.min.js`，或运行项目根目录的 `scripts/download_echarts.py`（若存在）。
